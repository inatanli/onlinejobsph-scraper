{
  "title": "OnlineJobs.ph Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "jobKeyword": {
      "title": "Job Keyword",
      "type": "string",
      "description": "Search keyword (e.g. virtual assistant, gtm). Leave blank to return all jobs.",
      "editor": "textfield",
      "prefill": "virtual assistant"
    },
    "maxPages": {
      "title": "Max Pages",
      "type": "integer",
      "description": "Maximum number of search result pages to crawl (30 jobs per page).",
      "default": 5,
      "prefill": 5,
      "minimum": 1,
      "editor": "number"
    }
  }
}
